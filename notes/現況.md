# いまの状況

> 新しいセッションが開いたら、まずここを読むと話が早い。
> 状況が変わったら書き換える（履歴ではなく「現在地」を書く場所）。

最終更新: 2026-08-27

---

## プロジェクトの状態

| リポジトリ | 状態 |
|---|---|
| `traffic-watch` | **稼働中**。平日15:10/15:30/15:45 にチェック → 異常時のみ Discord 通知。沈黙が正常 |
| `x-post` | **稼働中**。GitHub Actions で経済指標を X に自動投稿（平日 09:04 / 18:04） |
| `shinpuro` | **並行検証中**（下記） |
| `Aniki-no-Heya` | ここ。相談部屋 |

## shinpuro（物流データ変換）— いま検証中

伝票・配車データ → 抽出 → Python で座標付与 → Google Drive 納入、を
チャット＋iPhone Pyto から Claude Code に移している最中。

🎉 **2026-08-27、残っていた最後の関門（ホームズURL取得）が開通。
3ステップ全部が Claude 側で完結する状態になった。**

- **回帰テスト合格済み**（2026-08-24。実データで Pyto 版と出力が完全一致）
- **並行稼働で6回ほど検証してから移行判断**する方針。データは週3回なので約2週間
  - 260824（回帰）、260827（実施分）とも差分ゼロ。順調
- 🚨 **数量（M/S/ガムテ/クレ/エア）がこの仕事の急所。** 間違うと現場が止まる。
  住所・部屋番号も同様に間違いが許されない
- 検算ツール `verify.py` を用意済み（担当件数と明細行数の照合、新旧突合）
- 作業は `shinpuro` のセッションで行う。ここでは方針の相談だけ

**この件で相談されたら、詳細は `shinpuro` の CLAUDE.md にあると案内する。**

### 確定したこと（2026-08-25〜27）

- パイプライン末尾の **DB更新を Claude が担当**することに決定。実証済み
- **AL差分（旧・出力②）は廃止**
- ✅ **ホームズURL取得が開通**（2026-08-27）。環境のネットワーク設定を
  Custom にし `google.serper.dev` を許可 → 実キーで200・10件ヒット確認。
  取得URLもルーティン版と完全一致。詳細は `notes/ネットワーク到達性.md`
- **月次統合（差分→マスター）も Claude で実施可能**と確認。`tools/merge_stats.py`
  - 2026-08-25 実施：配送統計マスター 65日分 → **66日分**（260307〜260825）
  - ドライブは**中身の上書きだけが不可**。詳細は `notes/Googleドライブでできること.md`
- **差分ファイルは廃止。マスターを直接書き出す方式に一本化**（2026-08-25 決定）
  - 旧マスターは消さずに残す。日付入りファイル名で世代が並ぶので戻せる
  - 古い世代の整理は運用が安定してから判断（今は消さない）
  - 手順は `notes/DB更新の手順.md`
- **2026-08-26 通しテスト成功**（8/27配達分）。伝票Excel＋割当画像 → 統計行計算 →
  マスター260827版（67日分）をドライブへ。旧版は残置。今後もこの流れで運用
  - 入力がPDFでなくExcelで来ることがある。どちらでも対応可
  - 集荷総数10件は**オーナー確認済みで正しい**。マスターはそのままで良い

## これからの段取り（2026-08-27 オーナー決定）

```
1. もう1回データを通す        ← 次回。ここで問題なければ次へ
2. 実践投入                   ← チャットAIは残したまま、エージェントだけで運用
3. シンプロのセッションを使う   ← この部屋ではなく、向こうで日次を回す
4. GitHub 経由のデータ登録へ移行 ← 3が安定してから着手
```

- **チャットAI（Pyto版）は消さずに残す。** フォールバックとして温存
- 日次の作業場は `shinpuro` のセッションへ移す（この部屋は相談・設計に戻る）
- 4の「GitHub経由」は、ドライブ納入を GitHub Actions に逃がす話。
  トークン消費の最大要因がここなので、Proプランに戻る前にやる価値が高い

## 次回の確認ポイント

- 次のシンプロ処理で **Nインポート一覧表のホームズURL列が埋まること**を確認
  （以前は「未設定でスキップ」と入っていた）
- これが確認できたら **段取り2（実践投入）へ進む**

## 決まっている進め方

- **相談・設計はここ、実装は該当リポジトリのセッション**で分担する
- セッションは何個でも並行して開ける。待たせる必要はない
- 新しいプロジェクトも1つずつ潰していく方針（まとめてやらない）
- **日次運用は Opus でやる**（Fable はトークン単価が Opus の2倍）

## 直近で片付いたこと（2026-08）

- ntfy → Discord 移行（ntfy は Actions のIP共有と相性が悪く構造的に不可）
- 古い空リポジトリ2つを整理し、この部屋を開設
- Claude のデータ学習設定はオフであることを確認済み（顧客の個人情報を扱うため）
- **ホームズURL取得のネットワーク制約を解消**（2026-08-27）

## 積み残し

- `00_Apple稼働` に役目を終えたファイルが残っている。方針が固まるまで**消さずに放置でよい**：
  - `統計差分_260825.csv`（マスター 260825 に取り込み済み。差分方式自体を廃止）
  - `AL差分_260825.csv` / `AL確認マスター_260822.csv` / `AL索引_260823.csv`（AL 廃止で宙に浮いた）
  - 旧マスター `配送統計マスター_260822.csv` は**ロールバック用に意図的に残す**
- ドライブのファイル移動は Claude 側の権限判定で止まることがある。
  退避が必要なときは手作業で
- **BOM の付け外しがファイルごとにバラバラ**。過去に文字化け対策で BOM 付きにした経緯があり、
  工程ごとに担当AIが違ったため統一されなかった。読み込み側を `utf-8-sig` にすれば
  どちらでも壊れないので、当面は「読む側で吸収、書く側は BOMなし」で統一する
